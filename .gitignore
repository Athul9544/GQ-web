# Never commit the admin password.
data/admin-password.txt

# OS / editor noise.
.DS_Store
Thumbs.db
*.log

# No dependencies are used, but just in case.
node_modules/
.vercel

# Local mobile-preview harness, not part of the site.
_probe.html
